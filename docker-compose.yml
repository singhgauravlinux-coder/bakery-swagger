# Crumb & Ember — full local stack in one command:
#   cp .env.example .env   (optional; sane defaults are baked in)
#   docker compose up --build
#
# Frontend .......... http://localhost:8080
# API gateway ....... http://localhost:3000/api/...
# Adminer (DB UI) ... http://localhost:8081   (postgres / bakery / bakery)
name: crumb-and-ember

x-node-service: &node-service
  restart: unless-stopped
  environment: &node-env
    LOG_LEVEL: ${LOG_LEVEL:-info}
  healthcheck:
    test: ["CMD", "node", "-e", "fetch('http://localhost:3000/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
    interval: 15s
    timeout: 3s
    retries: 5
    start_period: 10s
  networks: [bakery]

services:
  # ------------------------------------------------------------ data layer
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-bakery}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-bakery}
      POSTGRES_DB: ${POSTGRES_DB:-bakery}
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-bakery} -d ${POSTGRES_DB:-bakery}"]
      interval: 10s
      timeout: 3s
      retries: 10
    networks: [bakery]

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: ["redis-server", "--maxmemory", "128mb", "--maxmemory-policy", "allkeys-lru", "--appendonly", "yes"]
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 10
    networks: [bakery]

  adminer:
    image: adminer:4
    restart: unless-stopped
    environment:
      ADMINER_DEFAULT_SERVER: postgres
      ADMINER_DESIGN: dracula
    ports:
      - "8081:8080"
    depends_on:
      postgres:
        condition: service_healthy
    networks: [bakery]

  # ------------------------------------------------------------------ edge
  frontend:
    build: ./services/frontend
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./services/frontend/nginx.compose.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      api-gateway:
        condition: service_healthy
    networks: [bakery]

  api-gateway:
    <<: *node-service
    build: ./services/api-gateway
    ports:
      - "3000:3000"
    environment:
      <<: *node-env
      SERVICE_NAME: api-gateway

  # --------------------------------------------------------- domain services
  auth-service:
    <<: *node-service
    build: ./services/auth-service
    environment:
      <<: *node-env
      SERVICE_NAME: auth-service
      DATABASE_URL: postgresql://${POSTGRES_USER:-bakery}:${POSTGRES_PASSWORD:-bakery}@postgres:5432/${POSTGRES_DB:-bakery}
      AUTH_TOKEN_SECRET: ${AUTH_TOKEN_SECRET:-local-dev-token-secret}
    depends_on:
      postgres:
        condition: service_healthy

  user-service:
    <<: *node-service
    build: ./services/user-service
    environment:
      <<: *node-env
      SERVICE_NAME: user-service
      DATABASE_URL: postgresql://${POSTGRES_USER:-bakery}:${POSTGRES_PASSWORD:-bakery}@postgres:5432/${POSTGRES_DB:-bakery}
    depends_on:
      postgres:
        condition: service_healthy

  product-catalog-service:
    <<: *node-service
    build: ./services/product-catalog-service
    environment:
      <<: *node-env
      SERVICE_NAME: product-catalog-service
      DATABASE_URL: postgresql://${POSTGRES_USER:-bakery}:${POSTGRES_PASSWORD:-bakery}@postgres:5432/${POSTGRES_DB:-bakery}
    depends_on:
      postgres:
        condition: service_healthy

  inventory-service:
    <<: *node-service
    build: ./services/inventory-service
    environment:
      <<: *node-env
      SERVICE_NAME: inventory-service

  pricing-service:
    <<: *node-service
    build: ./services/pricing-service
    environment:
      <<: *node-env
      SERVICE_NAME: pricing-service

  cart-service:
    <<: *node-service
    build: ./services/cart-service
    environment:
      <<: *node-env
      SERVICE_NAME: cart-service
      REDIS_URL: redis://redis:6379
    depends_on:
      redis:
        condition: service_healthy

  order-service:
    <<: *node-service
    build: ./services/order-service
    environment:
      <<: *node-env
      SERVICE_NAME: order-service
      DATABASE_URL: postgresql://${POSTGRES_USER:-bakery}:${POSTGRES_PASSWORD:-bakery}@postgres:5432/${POSTGRES_DB:-bakery}
    depends_on:
      postgres:
        condition: service_healthy

  payment-service:
    <<: *node-service
    build: ./services/payment-service
    environment:
      <<: *node-env
      SERVICE_NAME: payment-service
      DATABASE_URL: postgresql://${POSTGRES_USER:-bakery}:${POSTGRES_PASSWORD:-bakery}@postgres:5432/${POSTGRES_DB:-bakery}
      # Leave unset to run the mock provider locally.
      RAZORPAY_KEY_ID: ${RAZORPAY_KEY_ID:-}
      RAZORPAY_KEY_SECRET: ${RAZORPAY_KEY_SECRET:-}
      RAZORPAY_WEBHOOK_SECRET: ${RAZORPAY_WEBHOOK_SECRET:-}
    depends_on:
      postgres:
        condition: service_healthy

  delivery-service:
    <<: *node-service
    build: ./services/delivery-service
    environment:
      <<: *node-env
      SERVICE_NAME: delivery-service

  notification-service:
    <<: *node-service
    build: ./services/notification-service
    environment:
      <<: *node-env
      SERVICE_NAME: notification-service

  review-service:
    <<: *node-service
    build: ./services/review-service
    environment:
      <<: *node-env
      SERVICE_NAME: review-service

  search-service:
    <<: *node-service
    build: ./services/search-service
    environment:
      <<: *node-env
      SERVICE_NAME: search-service

  recommendation-service:
    <<: *node-service
    build: ./services/recommendation-service
    environment:
      <<: *node-env
      SERVICE_NAME: recommendation-service

  promotion-service:
    <<: *node-service
    build: ./services/promotion-service
    environment:
      <<: *node-env
      SERVICE_NAME: promotion-service

  loyalty-service:
    <<: *node-service
    build: ./services/loyalty-service
    environment:
      <<: *node-env
      SERVICE_NAME: loyalty-service

  recipe-service:
    <<: *node-service
    build: ./services/recipe-service
    environment:
      <<: *node-env
      SERVICE_NAME: recipe-service

  baking-schedule-service:
    <<: *node-service
    build: ./services/baking-schedule-service
    environment:
      <<: *node-env
      SERVICE_NAME: baking-schedule-service

  supplier-service:
    <<: *node-service
    build: ./services/supplier-service
    environment:
      <<: *node-env
      SERVICE_NAME: supplier-service

  analytics-service:
    <<: *node-service
    build: ./services/analytics-service
    environment:
      <<: *node-env
      SERVICE_NAME: analytics-service

  media-service:
    <<: *node-service
    build: ./services/media-service
    environment:
      <<: *node-env
      SERVICE_NAME: media-service

  invoice-service:
    <<: *node-service
    build: ./services/invoice-service
    environment:
      <<: *node-env
      SERVICE_NAME: invoice-service

networks:
  bakery:

volumes:
  pgdata:
  redisdata:
